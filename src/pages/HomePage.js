import React from 'react';
import { Typography } from '@material-ui/core';

const HomePage = () => {
    return <Typography variant='h5' align='center' >
        Bem vindo ao sistema de acompanhamento de temperatura!
    </Typography>
}

export default HomePage;